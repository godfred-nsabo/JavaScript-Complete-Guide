/*Assignment - Variables, Operators, Data Types*/

/*Create two variables: One that holds a fictional user input (a number of
our choice) and one that holds no value initially (e.g. "result")*/

const userInput = 0;
let result;

/*Set the result variable to 18 plus the value stored in user input.*/

result = 18 + userInput;

/*Add three additional lines of code where you change the result variable
again (by subtracting a value of your choice, multiplying it and dividing it).
*/
result = result - 15;
result = result * 3;
result = result / 15;

/*Think about the value stored in the user input variable you also created - did that value change?*/
 
 /* No, the value in the userInput did not change. It is the default value upon which the result value 
 is being computed*/

 // Printing the results using alert() function

 alert(result);
 alert(userInput);